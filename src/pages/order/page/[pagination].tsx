import React, { useEffect } from "react";

// routing for creating page with pagination
const OrderList = () => {
  const initData = async () => {
    try {
      let apiRes = "apiUrl";
    } catch (error) {}
  };

  useEffect(() => {
    initData();
  }, []);
  return <div></div>;
};

export default OrderList;
